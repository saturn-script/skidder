local autoSell = false
local autoBuy = false
local infmoneybap = false
local infspinbap = false
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")

local WindUI = loadstring(game:HttpGet(
    "https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"
))()


local Window = WindUI:CreateWindow({
    Title = "SaturnHub",
    Icon = "eye-off",
    Author = "by 1lsgw",
    Folder = "saturnhub",
    
    Size = UDim2.fromOffset(580, 460),
    MinSize = Vector2.new(560, 350),
    MaxSize = Vector2.new(850, 560),
    Transparent = true,
    Theme = "Violet",
    Resizable = true,
    SideBarWidth = 200,
    HideSearchBar = true,
    ScrollBarEnabled = false,
    NewElements = true,
 
    BackgroundImageTransparency = 0.42,
    Background = "rbxassetid://1234",
    
    User = {
        Enabled = true,
        Anonymous = true,
        Callback = function()
            Window.User:SetAnonymous(VAR)
            VAR = not VAR
        end,
    },
        KeySystem = {                                                   
        Note = "Example Key System. With pandadevelopment.",        
        API = {                                                     
            {
                Type = "pandadevelopment",
                ServiceId = "saturn",
            },                                                      
        },                                                          
    },                                                              
})

Window:EditOpenButton({
    Title = "SaturnHub",
    Icon = "eye-off",
    CornerRadius = UDim.new(0,16),
    StrokeThickness = 2,
    Color = ColorSequence.new( -- gradient
        Color3.fromHex("ddd6fe"), 
        Color3.fromHex("faf5ff")
    ),
    OnlyMobile = false,
    Enabled = true,
    Draggable = true,
})

--                        ↓ Special name     ↓ Icon     ↓ Callback                         ↓ Order
Window:CreateTopbarButton("KetaminButton", "code",    function() loadstring(game:HttpGet("https://raw.githubusercontent.com/InfernusScripts/Ketamine/refs/heads/main/Ketamine.lua"))() end,  990)
--                        ↓ Special name     ↓ Icon     ↓ Callback                         ↓ Order
Window:CreateTopbarButton("KetaminButton", "file",    function() loadstring(game:HttpGet("https://raw.githubusercontent.com/infyiff/backup/main/dex.lua"))() end,  990)

local HomePageTaB = Window:Tab({
    Title = "HomePage",
    Icon = "house",
})

local InviteCode = "PrTTxuCzjC"

local function GetDiscordInviteData(code)
    local url = "https://discord.com/api/v10/invites/" .. code .. "?with_counts=true"

    local success, response = pcall(function()
        return game:HttpGet(url)
    end)

    if not success then
        return nil, response
    end

    local decoded
    success, decoded = pcall(function()
        return HttpService:JSONDecode(response)
    end)

    if not success then
        return nil, decoded
    end

    return decoded
end

local Response, Error = GetDiscordInviteData(InviteCode)

if Response and Response.guild then
    local DiscordInfo = HomePageTaB:Paragraph({
        Title = Response.guild.name,
        Desc =
            ' <font color="#52525b">•</font> Member Count : ' .. tostring(Response.approximate_member_count) ..
            '\n <font color="#16a34a">•</font> Online Count : ' .. tostring(Response.approximate_presence_count),
        Image = not RunService:IsStudio()
            and ("https://cdn.discordapp.com/icons/" ..
                Response.guild.id .. "/" .. Response.guild.icon .. ".png?size=1024")
            or "rbxassetid://88876696246404",
        ImageSize = 42,
    })

    HomePageTaB:Button({
        Title = "Update Discord Info",
        Callback = function()
            local UpdatedResponse = GetDiscordInviteData(InviteCode)

            if UpdatedResponse and UpdatedResponse.guild then
                DiscordInfo:SetDesc(
                    ' <font color="#52525b">•</font> Member Count : ' .. tostring(UpdatedResponse.approximate_member_count) ..
                    '\n <font color="#16a34a">•</font> Online Count : ' .. tostring(UpdatedResponse.approximate_presence_count)
                )
            end
        end
    })
else
    HomePageTaB:Paragraph({
        Title = "Error when receiving information about the Discord server",
        Desc = Error and tostring(Error) or "No data",
        Image = "solar:info-circle-bold",
        ImageSize = 26,
        Color = "Red",
    })
end
local Keybind = HomePageTaB:Keybind({
    Title = "Keybind",
    Desc = "Keybind to open ui",
    Value = "G",
    Callback = function(v)
        Window:SetToggleKey(Enum.KeyCode[v])
    end
})
local Dropdown = HomePageTaB:Dropdown({
    Title = "Theme",
    Values = {
        "Dark",
        "Rose",
        "Plant",
        "Red",
        "Indigo",
        "Sky",
        "Violet",
        "Amber",
        "Emerald",
        "Midnight"
    },
    Value = "Violet",
    Callback = function(selected)
        WindUI:SetTheme(selected)
    end
})
local Slider = HomePageTaB:Slider({
    Title = " invisibility",
    Desc = "",
    Step = 0.01,
    Value = {
        Min = 0,
        Max = 1,
        Default = 0.1,
    },
    Callback = function(value)
        Window:SetBackgroundTransparency(value)
    end
})
HomePageTaB:Button({
    Title = "Join Discord server",
    Callback = function()
        local link = "https://discord.gg/" .. InviteCode

        if setclipboard then
            setclipboard(link)
        else
            WindUI:Notify({
                Title = "Discord Invite Link",
                Content = link,
            })
        end
    end
})



HomePageTaB:Button({
    Title = "Unload",
    Callback = function()
        Window:Destroy()
    end
})

--libarys

if game.PlaceId == 107095834793267 then
    
    
    
    local Tab = Window:Tab({
    Title = "OilEmpiere",
    Icon = "fuel", -- optional
})

Tab:Paragraph({
    Title = "Automatic",
    Desc = "Core functionality"
})
 
    local Toggle = Tab:Toggle({
    Title = "Enable AutoSell",
    Callback = function(state)
        autoSell = state

        if autoSell then
            while autoSell do
                game:GetService("ReplicatedStorage")
                    .Packages.Knit.Services.BaseService.RE.SellGas:FireServer()
                task.wait(0.1)
            end
        end
    end
})

local Dropdown = Tab:Dropdown({
    Title = "Select Value",
    Values = {
        "Option 1",
        "Option 2",
        "Option 3"
    },
    Value = "Option 1",
    Callback = function(selected)
        print("Selected:", selected)
    end
})

    local Toggle = Tab:Toggle({
    Title = "Enable autoBuy",
    Callback = function(state)
        autoBuy = state

        if autoBuy then
            while autoBuy do
                print ("test")
                task.wait(0.1)
            end
        end
    end
})

 
Tab:Divider()
 
Tab:Paragraph({
    Title = "Advanced Features",
    Desc = "Expert options"
})
elseif game.PlaceId == 137925884276740 then
    local Tab = Window:Tab({
    Title = "Build A Plane",
    Icon = "plane", -- optional
})

Tab:Paragraph({
    Title = "Automatic",
    Desc = "Core functionality"
})
 
    local Toggle = Tab:Toggle({
    Title = "Enable infmoneybap",
    Callback = function(state)
        infmoneybap = state

if infmoneybap then
    task.spawn(function()
        _G.aqua = _G.aqua or {}
        _G.aqua.superfarmer = true

        game:GetService("CoreGui").PurchasePromptApp.Enabled = false

        for i = 1, 300 do
            task.spawn(function()
                while _G.aqua.superfarmer and task.wait() do
                    game:GetService("ReplicatedStorage").Remotes.EventEvents.SpawnEvilEye:InvokeServer()
                    game:GetService("ReplicatedStorage").Remotes.EventEvents.KillEvilEye:InvokeServer()
                end
            end)
        end

        for i = 1, 80 do
            task.spawn(function()
                while _G.aqua.superfarmer and task.wait() do
                    game:GetService("ReplicatedStorage").Remotes.SpinEvents.PurchaseSpin:InvokeServer()
                    game:GetService("ReplicatedStorage").Remotes.SpinEvents.PerformSpin:InvokeServer()
                end
            end)
        end
    end)
else
    _G.aqua.superfarmer = false
end
    end
})

    local Toggle = Tab:Toggle({
    Title = "Enable infspinbap",
    Callback = function(state)
        infspinbap = state

        if infspinbap then
            while infspinbap do
                local args = { Instance.new("Part", nil) }
                game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("UIEvents"):WaitForChild("LightningBolt"):FireServer(unpack(args))
                task.wait(0.1)
            end
        end
    end
})

 


 
Tab:Divider()
 
Tab:Paragraph({
    Title = "Advanced Features",
    Desc = "Expert options"
})
end
